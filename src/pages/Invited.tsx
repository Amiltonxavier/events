import { Layout } from "../layout"
export function Invited() {
  //{ isInviteModalOpen && selectSingleEvent && <DiologCreatInvited eventID={selectSingleEvent.id} onClose={onCloseInviteDiolog} onInvited={onInvited}  /> }
  return (
    <Layout>
      <h1>Convidados</h1>
    </Layout>
  )
}
